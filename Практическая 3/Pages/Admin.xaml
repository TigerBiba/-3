<Page x:Class="Практическая_3.Pages.Admin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Практическая_3.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Admin">
    

    <Grid>

        <ListView Name="LViewStaff" Margin="5,5,5,5" d:ItemsSource="{d:SampleData ItemCount=1}">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" Background="{Binding Backgroud}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="120"/>
                                <RowDefinition Height="85"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>

                            <Image Width="100" Height="100" Margin="5" Source="{Binding photo}"/>

                            <StackPanel Grid.Row="1" Margin="0">
                                <TextBlock Text="{Binding speciality}" FontSize="12" TextAlignment="Center" />
                                <TextBlock FontSize="12" TextAlignment="Center">
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat='{}{0} {1}'>
                                            <Binding Path="firstname" />
                                            <Binding Path="secondname" />
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                                <TextBlock Text="{Binding email}" FontSize="12" TextAlignment="Center" />
                            </StackPanel>
                            
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel></WrapPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
    </Grid>
</Page>